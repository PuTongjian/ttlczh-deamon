# Electron Builder 构建环境变量配置示例
# 复制此文件为 .env.build 并根据需要修改
# 或者在构建前手动设置这些环境变量

# 禁用代码签名自动发现，避免下载 winCodeSign 工具
# 这可以解决 Windows 上符号链接权限错误
CSC_IDENTITY_AUTO_DISCOVERY=false

# 跳过代码签名（如果已设置 sign: false，此选项可选）
SKIP_NOTARIZATION=true

# 如果需要代码签名，取消注释并配置以下变量：
# CSC_LINK=path/to/certificate.p12
# CSC_KEY_PASSWORD=your_certificate_password

# Windows 代码签名相关（仅在需要签名时使用）
# WIN_CSC_LINK=path/to/certificate.p12
# WIN_CSC_KEY_PASSWORD=your_certificate_password

